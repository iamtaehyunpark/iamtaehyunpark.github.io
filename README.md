# Taehyun Park — portfolio

Static site (plain HTML/CSS/JS, no build step) served by GitHub Pages.

- Edit content in `index.html`; styles in `style.css`.
- Headshot: drop a square photo at `assets/headshot.jpg` (≥ 400×400). Until then a "TP" placeholder is shown.
- CV: `assets/Taehyun_Park_CV.pdf` (phone number intentionally omitted).
